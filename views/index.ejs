<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Campus Events 2025 - Marwadi University</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #1976D2;
      --primary-dark: #0D47A1;
      --accent: #FFC107;
      --white: #FFFFFF;
      --light-gray: #F5F5F5;
      --card-shadow: 0 2px 10px rgba(0, 0, 0, 0.08);
      --transition-smooth: all 0.3s ease;
    }

    * { margin:0; padding:0; box-sizing:border-box; }
    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--light-gray);
      color: #212121;
      overflow-x: hidden;
      line-height: 1.6;
    }

    /* Navbar */
    .navbar {
      display:flex; justify-content:space-between; align-items:center;
      padding:1rem 5%; background: var(--white);
      box-shadow:0 2px 8px rgba(0,0,0,0.12);
      position:fixed; width:100%; top:0; z-index:1000;
      transition: var(--transition-smooth);
    }
    .navbar.scrolled { padding:0.7rem 5%; }
    .logo { font-weight:700; font-size:1.2rem; color:var(--primary); }
    .links { display:flex; gap:1.5rem; }
    .links a {
      text-decoration:none; color:#555; font-weight:500; font-size:0.95rem;
      position:relative; padding:0.5rem 0; transition:var(--transition-smooth);
    }
    .links a::after {
      content:''; position:absolute; width:0; height:2px; bottom:0; left:0;
      background: var(--primary); transition: var(--transition-smooth);
    }
    .links a:hover { color:var(--primary); }
    .links a:hover::after { width:100%; }

    /* Hero Section with Light Image */
    .hero {
      height:100vh; display:flex; align-items:center; justify-content:center;
      padding:0 5%; position:relative; overflow:hidden; color:var(--white);
      background: url('https://images.unsplash.com/photo-1496307042754-b4aa456c4a2d?auto=format&fit=crop&w=1600&q=80') no-repeat center center/cover;
    }
    .hero::after {
      content: ''; position:absolute; top:0; left:0; width:100%; height:100%;
      background: rgba(0,0,0,0.45); /* overlay so text stands out */
    }
    .hero-content {
      max-width:800px; text-align:center; z-index:1; animation:fadeIn 1s ease-out;
    }
    .hero-content h1 {
      font-size:3.2rem; font-weight:700; margin-bottom:1.5rem;
      text-shadow:0 3px 8px rgba(0,0,0,0.3);
    }
    .hero-content p {
      font-size:1.1rem; line-height:1.7; margin-bottom:2rem; font-weight:400;
    }
    .hero-content strong { color:var(--accent); font-weight:600; }

    /* Buttons */
    .cta-buttons { display:flex; gap:1rem; justify-content:center; margin-top:1.5rem; }
    .btn-primary, .btn-secondary {
      padding:0.75rem 1.8rem; border-radius:6px; font-weight:600; font-size:0.95rem;
      text-decoration:none; transition: var(--transition-smooth);
    }
    .btn-primary {
      background: var(--accent); color: var(--primary-dark);
      box-shadow:0 3px 12px rgba(0,0,0,0.2);
    }
    .btn-primary:hover {
      background:#ffcf3d; transform:translateY(-3px);
      box-shadow:0 6px 18px rgba(0,0,0,0.25);
    }
    .btn-secondary {
      border:2px solid var(--white); color:var(--white); background:transparent;
    }
    .btn-secondary:hover {
      background:rgba(255,255,255,0.15);
      transform:translateY(-3px);
    }

    /* About Section */
    .about { padding:5rem 5%; background:var(--white); }
    .about h2 {
      text-align:center; font-size:2rem; margin-bottom:1rem;
      color:#212121; font-weight:600;
    }
    .about h2::after {
      content:''; display:block; width:60px; height:3px; background:var(--primary);
      margin:0.8rem auto 2.5rem; border-radius:2px;
    }
    .about-grid {
      display:grid; grid-template-columns: repeat(auto-fit,minmax(280px,1fr));
      gap:2rem;
    }
    .card {
      background: var(--white); border-radius:8px; padding:2rem; box-shadow: var(--card-shadow);
      transition: var(--transition-smooth); border-top:3px solid var(--primary); opacity:0;
    }
    .card.visible { animation:fadeIn 0.6s forwards; }
    .card:hover { transform:translateY(-5px); box-shadow:0 8px 20px rgba(0,0,0,0.15); }
    .card h3 { font-size:1.3rem; margin-bottom:0.8rem; color:var(--primary); font-weight:600; }
    .card p { color:#757575; line-height:1.6; font-size:0.95rem; }

    /* Footer */
    footer {
      background: var(--primary-dark); color: var(--white);
      text-align:center; padding:1.5rem 0; font-size:0.9rem; margin-top:2rem;
    }
    footer p { opacity:0.9; }

    /* Animations */
    @keyframes fadeIn {
      from { opacity:0; transform:translateY(15px); }
      to { opacity:1; transform:translateY(0); }
    }

    /* Responsive */
    @media(max-width:768px){
      .hero-content h1 { font-size:2.2rem; }
      .hero-content p { font-size:1rem; }
      .about h2 { font-size:1.8rem; }
      .cta-buttons { flex-direction:column; gap:0.8rem; }
    }
  </style>
</head>
<body>
  <!-- ✅ Navbar -->
  <nav class="navbar">
    <div class="logo">🎓 Marwadi University</div>
    <div class="links">
      <a href="/register">Register</a>
      <a href="/login">Login</a>
    </div>
  </nav>

  <!-- ✅ Hero Section with Image Background -->
  <section class="hero">
    <div class="hero-content">
      <h1>🌟 Campus Events 2025 🎉</h1>
      <p>
        Experience an unforgettable year of innovation, creativity, culture, and collaboration!  
        From <strong>Tech Symposiums 💻</strong> to <strong>Cultural Fests 🎶</strong> and <strong>Sports Meets 🏆</strong>,  
        Campus Events 2025 offers the perfect stage to connect, learn, and shine.
      </p>
      <div class="cta-buttons">
        <a href="/register" class="btn-primary">Register Now</a>
        <a href="/login" class="btn-secondary">Login</a>
      </div>
    </div>
  </section>

  <!-- ✅ About Section -->
  <section class="about">
    <h2>✨ Why Join Events?</h2>
    <div class="about-grid">
      <div class="card">
        <h3>🤝 Networking</h3>
        <p>Meet industry leaders, alumni, and peers. Build connections that last a lifetime.</p>
      </div>
      <div class="card">
        <h3>💡 Innovation</h3>
        <p>Showcase your creative ideas at hackathons, symposiums, and workshops.</p>
      </div>
      <div class="card">
        <h3>🎭 Culture & Fun</h3>
        <p>Be part of concerts, cultural nights, games, and memories you'll never forget.</p>
      </div>
    </div>
  </section>

  <!-- ✅ Footer -->
  <footer>
    <p>© 2025 Marwadi University. All Rights Reserved.</p>
  </footer>

  <script>
    // Navbar shrink on scroll
    window.addEventListener('scroll', () => {
      const navbar = document.querySelector('.navbar');
      if (window.scrollY > 50) navbar.classList.add('scrolled');
      else navbar.classList.remove('scrolled');
    });

    // Card reveal on scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('visible');
      });
    }, { threshold: 0.2 });
    document.querySelectorAll('.card').forEach(card => observer.observe(card));
  </script>
</body>
</html>